# 開発タスク一覧

## Phase 1: プロジェクトの初期設定
- [x] 開発用ディレクトリの作成とForge Modテンプレートのセットアップ (新規パスでの再設定)
- [x] `mods.toml` や `gradle` 等の基本設定 (指定バージョン用)

## Phase 2: プレイヤー登録と初期スポーン（平原）
- [x] プレイヤーの初回参加検知と参加者登録（最大6人）システムの実装
- [x] スポーン地点の探索（1000m離れた平原バイオームの座標特定ロジック）の実装
- [x] 初期聖域チャンクの割り当てと登録

## Phase 3: コアブロックと聖域防衛
- [x] メインコアブロック (`CoreBlock`) の実装・登録・テクスチャ・右クリック情報表示
- [x] コア破壊防止イベント (`CoreBlockEventHandler`) の実装
- [x] 初回参加時にコアブロックを自動設置する処理
- [x] サブコアブロックの実装と「聖域シールド（進入障壁）」の実装
  - [x] サブコアブロック (`SubCoreBlock`) の新規作成と登録
  - [x] サブコアのクラフトレシピ追加（金ブロック×4、泣く黒曜石×4、ダイヤ×1）
  - [x] サブコア設置時の検証（地上世界、水中でない、空が見える屋外、自領内）
  - [x] サブコアの設置状態をセーブデータ（`TerritorySavedData`）に永続保存
  - [x] サブコア生存時の敵進入障壁シールド（ノックバック弾き返し）の実装

## Phase 4: 領土管理とチャンク境界の動的可視化
- [x] 領土管理（チャンクと所有者のマッピング）およびセーブデータ保存の実装
- [x] プレイヤーのチャンク移動イベント検知と、進入時カラーパーティクル可視化演出の実装
- [x] アクションバー通知の実装
- [x] 開拓の旗ブロック（領土フラグ）の実装
  - [x] 開拓の旗ブロック (`TerritoryFlagBlock`) の新規作成と登録（松明風・当たり判定なし）
  - [x] 旗の設置時にそのチャンクを自領として登録する処理
  - [x] 旗の破壊時にそのチャンクを中立（未開拓）に戻す処理
  - [x] サブコアGUI（金両替＆旗購入機能）と専用ネットワーク通信の実装



## Phase 5: 属国化（吸収）と勝利判定
- [x] メインコア破壊時の「敗北＆属国化」イベントの実装
- [x] 属国の領土カラー統合およびリスポーン保護の検証
- [x] 全プレイヤー属国化時の勝利判定とアナウンス実装
- [x] 聖域リスポーン強制 (`PlayerRespawnEventHandler`) の実装
- [x] `/territory` コマンド（全体状態・自分情報・チャンク確認）の実装

## Phase 6: コアUI・ステータス画面の実装
- [x] 保存データ構造の拡張（`treasury`, `debuffLevel` の追加とNBT保存）
- [x] SimpleChannel を用いたパケット通信システム（`ModMessages` と各パケット）の実装
- [x] クライアントキー入力イベント（Yキーでステータス画面呼び出し）の実装
- [x] ステータス画面（`TerritoryStatusScreen`）の実装
- [x] コア画面（`CoreBlockScreen`）の実装とコア右クリック時の統合
- [x] テスト用初期ゴールド設定およびコマンド/FMLセットアップの統合
